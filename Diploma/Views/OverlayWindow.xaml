<Window x:Class="Diploma.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AlgoReplay Overlay"
        Width="220" Height="110"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Left="20" Top="20">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Border Background="#CC1E1E1E"
            CornerRadius="8"
            MouseLeftButtonDown="Border_MouseLeftButtonDown"
            Cursor="SizeAll">

        <Grid Margin="10,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0">
                <Ellipse x:Name="RecordingDot"
                         Width="8" Height="8"
                         Fill="#FF4444"
                         VerticalAlignment="Center"
                         Margin="0,0,6,0">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsRecording}" Value="True">
                                    <DataTrigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard RepeatBehavior="Forever">
                                                <DoubleAnimation
                                                    Storyboard.TargetProperty="Opacity"
                                                    From="1" To="0.2" Duration="0:0:0.8"
                                                    AutoReverse="True"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.EnterActions>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock Text="{Binding RecordingDuration, StringFormat={}{0:hh\\:mm\\:ss}}"
                           Foreground="White" FontSize="16"
                           FontWeight="Bold" FontFamily="Consolas"/>
            </StackPanel>

            <Button Grid.Row="0" Grid.Column="1"
                    Content="✕" Click="CloseButton_Click"
                    Background="Transparent" Foreground="#888"
                    BorderThickness="0" FontSize="10"
                    Cursor="Hand" Padding="4,0"/>

            <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="🎥 video:" Grid.Column="0"
                           FontSize="10" Foreground="#888"
                           VerticalAlignment="Center"/>

                <TextBlock x:Name="VideoTimerText"
                           Grid.Column="1"
                           Text="00:00:00"
                           FontSize="10" FontFamily="Consolas"
                           Foreground="#4CAF50"
                           VerticalAlignment="Center"/>

                <TextBlock x:Name="VideoStatusDot"
                           Grid.Column="2"
                           Text="●" FontSize="10"
                           Foreground="#888"
                           VerticalAlignment="Center"/>
            </Grid>

            <Grid Grid.Row="2" Grid.ColumnSpan="2" Margin="0,2,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="🎙 audio:" Grid.Column="0"
                           FontSize="10" Foreground="#888"
                           VerticalAlignment="Center"/>

                <TextBlock x:Name="AudioTimerText"
                           Grid.Column="1"
                           Text="00:00:00"
                           FontSize="10" FontFamily="Consolas"
                           Foreground="#4CAF50"
                           VerticalAlignment="Center"/>

                <TextBlock x:Name="AudioStatusDot"
                           Grid.Column="2"
                           Text="●" FontSize="10"
                           Foreground="#888"
                           VerticalAlignment="Center"/>
            </Grid>

            <TextBlock x:Name="DeltaText"
                       Grid.Row="3" Grid.ColumnSpan="2"
                       Text="Δ 0.0s"
                       FontSize="9" FontFamily="Consolas"
                       Foreground="#888"
                       Margin="0,3,0,0"/>
            
            <Button Grid.Row="4" Grid.ColumnSpan="2"
                    Content="📌 Маркер"
                    Margin="0,6,0,0"
                    Padding="4,2"
                    FontSize="10"
                    Click="MarkerButton_Click"
                    Visibility="{Binding IsRecording,
                     Converter={StaticResource BoolToVis}}"/>
        </Grid>
    </Border>
</Window>